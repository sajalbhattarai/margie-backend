# BSP API Environment Variables

# REQUIRED: Secret key for JWT token signing
# Generate a secure random key with: python -c "import secrets; print(secrets.token_urlsafe(32))"
BSP_SECRET_KEY=your-secret-key-here-change-this-in-production

# REQUIRED: Encryption key for sensitive data (32 url-safe base64-encoded bytes)
# Generate with: docker run --rm wintermutant/bsp-api:latest python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Or if you have cryptography installed locally: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
BSP_ENCRYPTION_KEY=your-encryption-key-here-change-this-in-production

# OPTIONAL: Database path (defaults to ./data/bsp.db)
# BSP_DB_PATH=/app/data/bsp.db

# OPTIONAL: MongoDB URI (if using MongoDB)
# MONGO_URI=mongodb://admin:password123@localhost:27017/

# OPTIONAL: Comma-separated list of allowed CORS origins for a public deployment.
# Defaults to "*" (any origin), which is what local dev relies on -- only set
# this once the frontend is hosted at a real, known domain.
# BSP_CORS_ALLOWED_ORIGINS=https://dane.anvilcloud.rcac.purdue.edu

# OPTIONAL: Local mode -- the API runs on the user's own computer instead of
# reaching an HPC cluster over SSH. The GUI's "Use MARGIE locally" option sets
# this itself when it starts the API; never set it on a hosted deployment.
# BSP_LOCAL_MODE=1

# OPTIONAL: Branch a hosted deployment keeps margie_sb's remote checkouts in
# sync with (see sync_remote_dane_wf). Only applies to margie_sb -- margie is
# wintermutant's own workflow and is never auto-synced. Defaults to
# "for-website-deployment"; once this branch is merged upstream, point this
# at whatever branch/tag wintermutant deploys from instead.
# BSP_MARGIE_SB_REF=for-website-deployment
